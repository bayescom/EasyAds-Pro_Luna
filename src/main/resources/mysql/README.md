创建名为easyads的数据库database，并使用文件夹下的sql语句创建相关库表。

**说明：**
1. 如果你是首次使用该项目，请直接用文件夹下的sql语句创建相关库表；
2. 如果你是从旧版本升级到 v2.0，请**务必**使用 v2.0 文件夹下的脚本进行升级；
3. 如果你要升级到 v2.3，直接运行 `v2.3/upgrade_db.py` 即可（会按当前版本自动补齐中间步骤，并写入版本号）。

